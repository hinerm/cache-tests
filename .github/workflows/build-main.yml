name: build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Cache test
      id: cache-test
      uses: actions/cache@v3
      with:
        path: test.txt
        key: ${{ runner.os }}-test

    - name: Make test file
      run: |
        echo "${{ github.run_id }}" >> test.txt

    - name: Print test file
      run: |
        cat test.txt

    - name: Delete cache
      if: steps.cache-test.outputs.cache-hit == 'true'
      run: |
        gh api --method DELETE -H "Accept: application/vnd.github+json" -f Key=${{ runner.os }}-test /repos/hinerm/cache-tests/actions/caches
      env:
        GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}

